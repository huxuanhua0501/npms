var SSO_COOKIE_URL = URL_UMS + "token.action";
//window.document.domain = "young.com";//设置域,与单点无关
/*jo.postJSONP(SSO_COOKIE_URL, function(json){
    var token = json.token;
    console.info("token = " + token);
});*/
